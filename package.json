{
  "name": "json-designer",
  "version": "1.0.0",
  "description": "A web application to model JSON data.",
  "scripts": {
    "css": "npx tailwindcss -i src/custom.css -o src/style.css --watch",
    "dev:css": "concurrently \"npx tailwindcss -i src/custom.css -o src/style.css --watch\" \"npx twlit --input ./src/style.css --output ./src/style.css.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx vite build"
  },
  "type": "module",
  "author": "Daniel Abgottspon",
  "license": "UNLICENSED",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "concurrently": "^9.1.2",
    "esbuild": "^0.24.2",
    "esbuild-copy-files-plugin": "^1.2.1",
    "lit": "^3.2.1",
    "sortablejs": "^1.15.6",
    "tailwindcss": "^3.4.17",
    "vite": "^6.0.11"
  },
  "devDependencies": {
    "twlit": "^0.0.1-alpha.37"
  }
}
